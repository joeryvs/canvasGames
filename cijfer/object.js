class Object {
  constructor(num) {
    this.werk = num;
    this.result = cijfer(num);
  }
  getcanvasCordinates(canvas = canvv1) {
    const h = canvv1.height;
    const w = canvv1.width;
    const ans = {};
    ans.x = this.werk;
  }
}
